import { InvalidControlScrollDirective } from './ngx-scroll-to-invalid-control.directive';

describe('NgInvalidControlScrollDirective', () => {
	it('should create an instance', () => {
		const directive = new InvalidControlScrollDirective();
		expect(directive).toBeTruthy();
	});
});
